import torch
import torch.nn as nn

import pyro


class LinearRegressor(nn.Module):
    '''Linear Regression for strings with 2nd order effects'''

    def __init__(self, space_dim):
        super(LinearRegressor, self).__init__()
        self.linear = nn.Linear(space_dim + space_dim*space_dim, 1)

    def format(x):
        
    def forward(x):
        pass
    
        
        
        
    