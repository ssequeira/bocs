import torch
import torch.nn as nn
import torch.distributions.constraints as constraints

import pyro
import pyro.distributions as dist


def centeredNormal(scale):
    return dist.Normal(torch.zeros(0), scale)

def 
    
    
